# Own control app
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.17  # FastAPI's UploadFile (POST /api/upload) needs this

# Drives the vendored GoogleFindMyTools Auth/ chain directly (see
# run_flow.py) -- the same full list as the root requirements.txt (this
# vendors the identical checkout, so needs the identical import closure).
# Confirmed empirically that trimming this list breaks a real run: an
# actual login attempt failed with "No module named 'aiohttp'" (from the
# owner_key/EID-info request path) when it was left out.
undetected-chromedriver>=3.5.5
selenium>=4.27.1
gpsoauth>=1.1.1
requests>=2.32.3
beautifulsoup4>=4.12.3
pyscrypt>=1.6.2
cryptography>=43.0.3
pycryptodomex>=3.21.0
ecdsa>=0.19.0
pytz>=2024.2
protobuf>=5.28.3
httpx>=0.28.0
h2>=4.1.0
setuptools>=75.6.0
aiohttp>=3.11.8
http_ece>=1.1.0
